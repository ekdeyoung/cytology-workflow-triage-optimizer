# Current Development Checkpoint

Read `docs/PROJECT_CONTEXT.md` first. It remains the authoritative source for product scope, terminology, and clinical workflow decisions.

This file records the current implementation checkpoint and immediate development task.

## Current Objective

Finish integrating clinical workflow-routing metadata into the existing Streamlit dashboard without changing the core AI-assisted cytology triage behavior.

The dashboard should display, for a selected case:

* Specimen Category
* Workflow Type
* Current Stage
* Next Stage
* Next Required Action

The workflow metadata is generated by `add_workflow_metadata()` in `src/workflow_engine.py`.

## Current Files Involved

* `src/dashboard.py`
* `src/workflow_engine.py`
* `src/triage_utils.py`
* `src/config.py`
* `data/raw/cytology_daily_metrics.csv`

## Completed Work

* Added cytology specimen categories:

  * `gynecologic`
  * `non_gynecologic`

* Added workflow types:

  * `routine`
  * `rose`

* Removed the need for a separate FNA workflow type.

* Added laboratory processing between ROSE adequacy assessment and primary cytologist screening.

* Replaced concordance terminology with discrepancy terminology.

* Added workflow metadata fields to the SQLite cases table:

  * `specimen_category`
  * `workflow_type`
  * `current_stage`
  * `screening_result`
  * `selected_for_quality_control`
  * `discrepancy_review_status`

* Added `add_workflow_metadata()` to `src/workflow_engine.py`.

* Added selected-case workflow routing information to `src/dashboard.py`.

* Changed user-facing terminology from Imager QC Review to Imager Review.

* Preserved the distinction between Imager Review and cytology Quality Control Review.

* Changed the legacy attention-state display label from Pathologist Review to Priority Review while preserving the internal `pathologist_review` value.

## Last Known Code State

The most recent `src/dashboard.py` version was being repaired after indentation errors caused by inserting the selected-case workflow-routing section.

The intended structure inside the `if filtered_queue.empty ... else:` block is:

1. Selected Case Summary
2. Clinical Information and Workflow Status
3. Clinical Workflow Routing
4. Predictive Assessment
5. Recommended Action
6. Workflow Action Center
7. Operational Worklist
8. Filtered queue export

All of these sections must remain inside the non-empty queue `else:` block and at the same indentation level, except for their own nested `with` and `if` blocks.

## Known Compatibility Concern

The trend-data terminology was partially changed from:

`qc_review_cases`

to:

`imager_review_cases`

The dashboard now expects `imager_review_cases`, but the existing file `data/raw/cytology_daily_metrics.csv` may still contain `qc_review_cases`.

Do not silently suppress this mismatch by allowing trend analytics to become empty.

Inspect the CSV header before changing anything.

## Immediate Next Development Step

Verify that the current dashboard source compiles, then identify the first runtime failure without making additional terminology or architecture changes.

Run:

```bash
python3 -m py_compile \
    src/dashboard.py \
    src/workflow_engine.py \
    src/triage_utils.py \
    src/triage_logic.py \
    src/predictive_features.py \
    src/database.py
```

If compilation succeeds, inspect the trend-data header:

```bash
head -n 2 data/raw/cytology_daily_metrics.csv
```

Then run:

```bash
streamlit run src/dashboard.py
```

Report the first error exactly as displayed.

If the dashboard opens successfully, verify that:

* The Overview tab loads.
* The Operational Queue tab loads.
* A case can be selected.
* Clinical Workflow Routing appears below the selected case summary.
* The Next Stage and Next Required Action are populated.
* The Operational Worklist remains visible below the action center.
* Trend Analytics displays historical data instead of an empty-data notice.

## Change Restrictions

Do not perform broad renaming.

Do not redesign the dashboard.

Do not change workflow templates.

Do not alter AI priority calculations.

Do not replace legacy internal field names merely for cosmetic consistency.

Do not modify multiple files until the first concrete compile or runtime defect has been identified.

## Required Response Format

After inspecting this checkpoint and the repository, provide only:

1. The first concrete defect found
2. The exact file
3. The exact lines or block to change
4. The smallest replacement
5. The verification command
6. The expected result

Do not ask the user what they were working on. It is documented here.
